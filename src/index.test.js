import Table, { TreeTable } from '.'

describe('Table', () => {
  it('is truthy', () => {
    expect(Table).toBeTruthy()
  })
})

describe('TreeTable', () => {
  it('is truthy', () => {
    expect(TreeTable).toBeTruthy()
  })
})
